function countSpaces(str) {
    let count = 0;
    for (let i = 0; i < str.length; i++) {
        if (str[i] === ' ') {
            count++;
        }
    }
    return count;
}

let myString = "Count the number of spaces in this string.";
console.log("Number of spaces:", countSpaces(myString));
