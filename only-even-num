function evennumber(arr)
{
    let i;
    for(i = 0; i < arr.length; i++)
    {
        if(arr[i]%2!=0)
        return false
    }
    return true
}
let arr = [2,3,22,32];
console.log(evennumber(arr));
