function maximum (arr)
{
    let i;
    let max = arr[0];
    for (i = 1; i < arr.length; i++)
    {
        if(arr[i] > max)
        max = arr[i]
    }
    return max
}
let arr = [3,2,1,3];
console.log("Maximum array element is" , maximum(arr));
