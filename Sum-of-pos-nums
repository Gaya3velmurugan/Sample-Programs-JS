function positive(arr)
{
    let i;
    let sum =0;
    for (i=0;i<arr.length;i++)
    {
        if(arr[i]>0)
        sum = sum + arr[i]
    }
    return sum;
}
let arr =[-1,-2,3,4];
console.log("Sum of positive element in array is", positive(arr));
